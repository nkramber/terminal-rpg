# Reviews

One file per PR: `pr-<number>.md` holds the review, and `pr-<number>-response.md` holds the author's answer (D-17). The number is the GitHub PR number. Audits and audit responses take a dated name. These files are dated records, so the STE checker skips them. The `pr-review` skill gives the format.
